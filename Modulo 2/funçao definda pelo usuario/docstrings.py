# Os docstrings são cadeias de texto que descrevem o propósito, os parâmetros e o valor de retorno de uma função. 
def area_sala(base, altura):
   #"""
   #Calcula a area da sala
    Args:
    base(float):A base da sala
    altura(float):a altura da sala
    return:
float: a area da sala
   # """
   return base * altura
